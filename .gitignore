node_modules/
dist/

# Built artefacts and caches. The extracted text under data/text/ IS committed
# (it is the expensive, reproducible part); the database and any retained PDFs
# are rebuilt from it.
data/site.db
data/pdf/
*.log
.DS_Store
.env

# Vendored SQLite WASM runtime (regenerated by "npm run vendor")
site/vendor/

# Generated at build time by src/cli/build.js
site/feed.xml
